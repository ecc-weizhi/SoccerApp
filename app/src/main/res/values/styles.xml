<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="PlayersButtonThemeOverlay" parent="ThemeOverlay.AppCompat.Light">
        <item name="colorAccent">@color/green_800</item>
    </style>

    <style name="PlayersButtonStyle" parent="Widget.AppCompat.Button.Colored">
        <item name="android:theme">@style/PlayersButtonThemeOverlay</item>
        <item name="colorButtonNormal">@color/green_100</item>
    </style>

</resources>
